version: '3.4'

services:
  catalogdb:
    image: mongo
    
  identitydb:
    image: mcr.microsoft.com/azure-sql-edge:latest
  
  catalog.api:
    image: catalog.api
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile
      
volumes:
  catalog_data:
